@model IEnumerable<Student>
@{
    ViewData["Title"] = "Index";
}

<h1 class="display-4 alert alert-info">Student List</h1>
<div class="d-flex justify-content-end mb-1">
    <a asp-action="Create" asp-controller="Students" class="btn btn-outline-primary btn-sm"><i class="fa fa-plus-circle"></i></a>
</div>

@foreach (var item in Model)
{
    <div class="card mb-4">
        <div class="card-header d-flex justify-content-between">
            <div>
                <img src="@item.Image" width="35" class="rounded-circle" />
                <b> Name : </b>@Html.DisplayFor(x => item.StudentName)
                <b> Date of Birth : </b>@Html.DisplayFor(x => item.DateOfBirth)
                <b> Phone : </b>@Html.DisplayFor(x => item.Phone)
                <b> MorningShift? : </b>@Html.DisplayFor(x => item.MorningShift)
            </div>
            <div>
                <a asp-action="Edit" asp-controller="Students" class="btn btn-outline-success btn-sm" asp-route-id="@item.StudentId"><i class="fa fa-pencil"></i></a>
                <a asp-action="Delete" asp-controller="Students" class="btn btn-outline-danger btn-sm" asp-route-id="@item.StudentId"><i class="fa fa-trash"></i></a>
            </div>
        </div>
        <div class="card-body">
            <table class="table table-bordered table-striped table-responsive-sm">
                <thead class="table-primary">
                    <tr>
                        <th>Subjects</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var i in item.StudentSubjects)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(x=>i.Subjects.SubjectName)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}
