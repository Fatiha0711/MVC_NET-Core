@model Subject
<div class="row d-flex justify-content-between mt-2">
    <div class="col-10">
        @Html.DropDownListFor(x=>x.SubjectId,ViewBag.subject as SelectList,"----Select----",new{@class="form-select form-select-sm"})
    </div>
    <div class="col-2">
        <a id="btnDeleteSubject" class="btn btn-danger btn-sm"><i class="fa fa-trash"></i></a>
    </div>
</div>